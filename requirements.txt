# Single virtual environment for obfusloc scripts
# Covers: OpenCV, DiffusionEdge, Mask2Former, SAM1, SAM2, Metric3D, pycolmap, PoseLib, hloc, RoMa
#
# Python 3.12
#
# torch and torchvision are NOT listed here — install them first with the
# wheel matching your CUDA version (see README).
#

# SAM2
sam2==1.1.0

# SAM1
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git@dca509fe793f601edb92606367a655c15ac00fdf

# Mask2Former + detectron2
mask2former @ git+https://github.com/spatial-intelligence-group/mask2former_package.git@e6ad4a4148f65fc92639bc31ec8a3cf4da275260
# detectron2 is pulled transitively by mask2former; install with --no-build-isolation

# Metric3D (loaded via torch.hub; mmcv requires --no-build-isolation)
mmengine
mmcv

# pycolmap + PoseLib
pycolmap==4.0.4
poselib==2.0.5


# hloc - pinned to abb2520 — last commit containing EigenPlaces.
hloc @ git+https://github.com/cvg/Hierarchical-Localization.git@abb252080282e31147db6291206ca102c43353f7
# RoMa matcher
romatch==0.1.2
fused-local-corr==0.2.4

# OpenCV
opencv-python==4.13.0.92

# DiffusionEdge runtime deps (denoising_diffusion_pytorch comes from third_party/DiffusionEdge)
accelerate==1.13.0
timm==1.0.27
einops==0.8.2
ema-pytorch==0.7.9
fvcore==0.1.5.post20221221
albumentations==2.0.8
omegaconf==2.3.0
pytorch-lightning==2.6.5
scipy==1.17.1
scikit-learn==1.8.0
thop==0.1.1.post2209072238
tensorboard==2.20.0
lpips==0.1.4
requests==2.34.2

# General utilities
numpy==2.4.4
faiss-cpu==1.14.2
h5py==3.16.0
tqdm==4.67.3
matplotlib==3.10.9
scikit-image==0.26.0
pillow==12.2.0
